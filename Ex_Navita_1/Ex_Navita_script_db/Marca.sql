create table Patrimonio( id int primary key not null identity(1,1),
                    nome varchar(100) not null,
                    marcald varchar(150) not null,
                    ativo bit not null)